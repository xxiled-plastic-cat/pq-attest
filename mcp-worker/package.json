{
  "name": "@pq-attest/mcp-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker remote MCP server for pq-attest",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251105.0",
    "@types/node": "^26.6.2",
    "tsx": "^4.21.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.137.0"
  }
}
